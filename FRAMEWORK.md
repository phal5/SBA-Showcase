# SBA — 코딩 규칙

## 1. 기본

- 파일 하나에 타입 하나, 타입 하나에 책임 하나.
- 파일명 = 타입명 = 명사구.
- 이름이 명사구로 안 떨어지면 쪼갠다. 단, **함께 지켜야 하는 것을 갈라놓지 않는다.**
- 공개 진입점은 타입당 하나.
- 폴더는 **도메인**으로 나눈다. 유형으로 나누지 않는다 (`Fragments/`, `Pipelines/` 금지).

## 2. 여덟 종류

| 종류 | 접미사 | 하는 일 | 하지 않는 일 | 상한 |
|---|---|---|---|---|
| 인덱스 | `Index` | 요청 → 파이프라인 목차 | 로직, `if`/`for`/`switch` | — |
| 파이프라인 | `Pipeline` | 한 기능의 순서 | 조건 분기, 계산 | 90줄 |
| 교차로 | `Branch` | 조건에 따라 다음 파이프라인 고르기 | 계산 | 갈래당 2문장 |
| 파편 | `Fragment` | 계산·검증 (순수 함수) | 상태 변경, `try/catch` | 40줄 |
| 트리거 | `Trigger` | 완료 후 다음 작업 적립 | 스레드 생성, 결과 대기 | 40줄 |
| 자원 | `Resource` | 소켓·풀·큐·카운터 등 살아 있는 상태 | 계산, 판단 | 110줄 |
| 레코드 | 없음 | 오가는 불변 값과 그 조건 | 분기 | — |
| 장부 | `Ledger` | 신고 받아 쌓기 | 판단 | 60줄 |

상한은 코드 줄만 센다. 주석·빈 줄은 안 센다.
레코드·교차로·인덱스에 줄 상한이 없는 이유: 크기가 도메인이 주는 수(필드 수, 갈래 수,
진입점 수)에 비례하기 때문. 대신 각각 분기 금지 · 갈래당 문장 상한 · 제어 흐름 금지.

접미사는 동작하는 것에만 붙인다. `enum`, `record struct`, 상수 집합은 그냥 명사
(`DropReason`, `TcpOpcode`).

## 3. 흐름

```
인덱스 → 파이프라인 ─ 1단계 → 파편
                    ├ 2단계 → 자원
                    ├ 3단계 → 교차로 ─┬→ 하위 파이프라인 A
                    │                   └→ 하위 파이프라인 B
                    └ 끝   → 트리거 → 다음 작업 적립
```

파이프라인은 위에서 아래로 한 줄이다. 단계는 전부 실행된다. **갈라지는 곳은 교차로뿐.**

## 4. 만드는 순서

1. 파이프라인 이름만 먼저 짓는다. 본문은 안 쓴다.
2. 파편으로 채운다.
3. 이름을 다시 검사한다.

먼저 이름 짓지 않으면 흐름이 남는 것이 되고, **순서에 근거를 둔 요구사항이 갈 곳을 잃는다.**
("중복은 완성을 앞당기지 않는다"의 근거는 파편이 아니라 파이프라인의 순서다.)

## 5. 각 종류별 규칙

**파이프라인**
- `switch` 없음. 즉시 반환·중단하는 가드만 허용.
- 단계마다 `// 1.` `// 2.` 번호.
- 다른 도메인의 인덱스를 부르지 않는다.
- 길어지면 뒷부분을 하위 파이프라인으로 자른다.

**교차로**
- 갈래가 **서로 다른 블록 호출**이면 교차로, **같은 계산의 인자 차이**면 파편.
- 산술·비트연산이 있으면 파편으로 옮긴다.
- 고른 갈래를 자기 안에서 신고한다.
- 갈래는 이름 붙은 열거형으로 가른다.

**자원**
- 상태를 들고 좁은 동사만 노출한다.
- 안에서 `if`가 값을 고르면 파편으로, 흐름을 고르면 교차로로 옮긴다.
- 락은 자원의 것이지만 락이 지키는 조건의 계산은 밖에 둔다.
- 내부 상태를 같은 도메인 파이프라인에 그대로 노출한다.

**레코드**
- 값이 만족해야 할 조건을 **이 파일이 진술하고, 이 파일이 검사한다.** 진술만 하고
  검사하지 않으면 안 한 것과 같다.
- 수량에는 **무엇을 세는지와 단위**를 이름에 적는다. "점유량"이 아니라
  "점유 바이트"와 "점유 슬롯 수". 이름에 없는 것은 아무도 세지 않는다.
- 실제로 잡히는 메모리를 센다. 요청한 크기가 아니라 풀이 내준 크기.

**트리거**
- 깨운 흐름을 기다리지 않는다.
- 스레드나 타이머를 만들지 않는다. 다음 호출 때 처리할 작업을 쌓아 둘 뿐이다.

**장부**
- 거부는 **그것을 낸 블록이** 신고한다. 한곳에 모아 적으면 빠뜨려도 컴파일된다.
- 사유만 받고 설명 없이 거절할 수 있는 함수를 남기지 않는다.

## 6. 파일 헤더

모든 파일은 정확히 이 3줄로 시작한다.

```csharp
// BLOCK: <종류> · <도메인>
// DOES: <접속사 없는 한 문장>
// PURE: yes | no
```

`PURE: yes`면 가변 필드·I/O·시계·락이 없다.
`USED-BY:` 같은 역참조는 넣지 않는다. 유지되지 않는다.

## 7. 이름

- 타입명: 명사구 1~3단어 + 접미사. 짧게.
- 공개 메서드명: 완결된 문장. 길게.
- 지역 변수: 축약 금지. `len` → `frameLength`.
- 부울: `is`/`has`/`must`로 시작.
- **찾기만 하는 함수와 없으면 만드는 함수를 이름으로 구분한다.**
  `FindX`/`ReadX`는 찾기만, `GetOrCreateX`/`OpenX`/`ReserveX`는 만든다.
  만드는 것에는 상한과 회수 경로가 있어야 한다.

## 8. 하지 말 것

- 검사하기 전에 버퍼에 쓰기 (예외가 나고, 뒤의 검사가 죽는다)
- 정리·회수를 배달·소비보다 먼저 부르기
- "읽기 전용"이라고 주석만 달기 — 변경 함수를 막아라
- 구현이 하나뿐인 `interface`
- 상속 2단 초과, `partial class`, 확장 메서드로 로직 숨기기
- `Utils`, `Helpers`, `Common`, `Misc`

## 9. 검사

- **검사는 그 코드가 참이라고 믿는 것을 깨는 입력을 스스로 만들어야 한다.**
  정상 입력만 만드는 검사는 정상 경로만 지킨다.
- 관계식 단언 하나마다 절대값 단언 하나를 함께 둔다. 관계식만 있으면 값이 조용히 틀린다.
- 모든 검사기를 일부러 깨뜨려 본다. **"0 위반"은 규칙이 지켜졌다는 뜻일 수도,
  검사기가 아무것도 안 보고 있다는 뜻일 수도 있다.**
- 파이프라인은 단계별 중간값을 선택적으로 기록한다(기본 꺼짐). 어느 단계에서 틀어졌는지
  좁힐 때 쓴다. 중간값을 남길 수 없는 곳(비밀 재료)에서는 독립 경로로 다시 계산해 결과만
  대조한다.

## 10. 주석과 문서

- 산문에 숫자를 적지 않는다. 이름을 적는다. `1,150` 대신 `P_floor`.
- 주석이 보장을 말하면 그 보장을 검사하는 단언이 있어야 한다. 없으면 주석을 지운다.
- 어떤 파일도 "안 열어 봐도 되는 것"으로 정하지 않는다. 건너뛰기로 정해진 자리가
  결함이 숨는 자리가 된다.

## 11. 마지막

```bash
python tools/sba_lint.py <소스경로>
```

린터가 검사하는 것: 헤더 3줄 · 접미사와 종류 일치 · `DOES`의 접속사 · 줄 상한 ·
레코드의 분기 · `PURE: yes` 파일의 부수효과 · 공개 진입점 1개 · 파이프라인의 `switch` ·
갈래에서 다른 블록을 부르는 파편(그건 교차로다) · 교차로의 계산 · 교차로의 신고 누락 ·
트리거의 대기 · `partial` · 요약 불가능한 파일명.

**린터 없이는 규칙이 미끄러진다.**
