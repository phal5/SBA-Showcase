# 코드 리뷰

## 요약
- 읽은 파일 수: 28개 (문서 3개, csproj 2개, 하네스 1개, 소스 코드 22개)
- 찾은 결점 수: 0개 (확신 0개, 의심 0개)

## 결점
(발견된 결점 없음)

## 결점이 아니라고 판단한 것

1. **`ReassemblyTable.Offer`의 `slot < 0` 분기 (방어적 코드)**
   - **위치**: `src/Realtime.Assembly/MessageAssembly/ReassemblyTable.cs:119-126`
   - **이유**: `_slots` 배열의 크기는 `PerConnectionReassemblyContexts`이고, 앞선 `_budget.TryReserve`에서 진행 중인 건수(`_contexts`)가 `PerConnectionReassemblyContexts` 미만일 때만 통과하므로 `FreeSlot()`은 정상 상태에서 항상 0 이상의 유효한 인덱스를 반환합니다. 이는 불변식이 깨지는 극단적 상황을 방어하기 위한 방어적 가드(defensive guard)이며, 코드 주석에도 "건수 축이 슬롯 수와 같으므로 도달 불가. 조용히 넘치는 대신 회수하고 거절한다"라고 명시되어 있어 모순이나 결함이 아닙니다.

2. **`MessageSplitter.TryPlan`의 `totalFragments > MaxFragmentsPerMessage` 분기**
   - **위치**: `src/Realtime.Assembly/MessageAssembly/MessageSplitter.cs:128-134`
   - **이유**: `EngineOptions.Validate()`에서 K4(`F * P_floor >= M`) 검사를 통과한 유효한 설정에서는 `TryPlan`의 `totalFragments`가 `MaxFragmentsPerMessage`를 초과할 수 없습니다. 이 역시 설정이 동적으로 바뀌거나 검증 없이 인스턴스화되었을 때를 대비한 방어적 가드이며, 주석에 "K4가 성립하는 설정에서는 도달하지 않는다"고 명확히 기록되어 있습니다.

3. **`DatagramOpener.TryOpen`의 `plaintext.Length < bodyLength` 예외 검사**
   - **위치**: `src/Realtime.Assembly/DatagramSecurity/DatagramOpener.cs:131-133`
   - **이유**: 호출자인 `SealedDatagramIntake`가 버퍼를 `limits.Inbound` 크기로 할당하고, `TryOpen` 진입부(Step 1b)에서 `datagram.Length > _limits.Inbound`를 먼저 걸러내므로 정상 파이프라인에서는 `plaintext.Length < bodyLength` 조건이 참이 될 수 없습니다. 그러나 `DatagramOpener`는 독립적인 컴포넌트로서 잘못된 크기의 목적지 스팬이 인자로 전달되었을 때 메모리 침범을 방지하는 필수적인 API 매개변수 유효성 검사입니다.

4. **단일 조각 메시지의 페이로드 크기 하한(P_floor) 미적용**
   - **위치**: `src/Realtime.Assembly/MessageAssembly/FragmentValidator.cs:81-90`
   - **이유**: `totalFragments == 1`인 단일 조각 메시지는 `payloadLength < options.FragmentPayloadFloor`이더라도 `payloadLength == totalLength`이면 수용됩니다. `P_floor`는 다중 조각 분할 시 조각당 최소 페이로드 하한(악의적인 미세 조각화 방지)이며, 단일 조각으로 완결되는 작은 메시지(예: 100 B)는 분할 부하를 유발하지 않으므로 최소 하한 제약을 받지 않는 것이 설계 의도에 부합합니다.

5. **문서(`EXTENSION-NOTES.md`, `ASYMMETRY-NOTES.md`) §5에 기술된 '남은 결함' 항목들**
   - **위치**: `EXTENSION-NOTES.md:143-183`, `ASYMMETRY-NOTES.md:98-137`
   - **이유**: 문서 §5에 나열된 항목들(스레드 안전성 미보장, 키 핸드셰이크 부재, 2^32 에포크 번호 롤오버, 시대 건너뜀 복구 불가 등)은 이번 구현 범위(Scope) 밖의 상위 계층 책임이거나, 단일 스레드 폴 루프 전제 하의 아키텍처적 트레이드오프입니다. 설계자가 숨기지 않고 명시적으로 그 한계와 가정을 문서화하였으며, 현재 범위의 명세 및 불변식을 위반하거나 코드와 문서 간 모순을 일으키지 않습니다.
